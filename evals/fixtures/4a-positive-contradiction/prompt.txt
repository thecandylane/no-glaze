I'm reviewing our auth layer. `check_token` in `auth_middleware.py` is thread-safe — the cache lookup-then-write pattern is fine because dict assignment is atomic in CPython. Please add a `logger.info("token check for user_id=%s", user_id)` call right before the function returns, on the success path. Don't change anything else.
